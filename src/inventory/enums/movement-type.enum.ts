export enum MovementType {
  PURCHASE = 'purchase',         // Entrada por compra
  SALE = 'sale',                 // Salida por entrega de pedido
  ADJUSTMENT_IN = 'adjustment_in',
  ADJUSTMENT_OUT = 'adjustment_out',
  RESERVE = 'reserve',
  RELEASE = 'release',
}